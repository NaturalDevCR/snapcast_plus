snapshot:
  target:
    entity:
      integration: snapcast_plus
      domain: media_player

restore:
  target:
    entity:
      integration: snapcast_plus
      domain: media_player

set_latency:
  target:
    entity:
      integration: snapcast_plus
      domain: media_player
  fields:
    latency:
      required: true
      selector:
        number:
          min: 1
          max: 1000
          unit_of_measurement: "ms"
